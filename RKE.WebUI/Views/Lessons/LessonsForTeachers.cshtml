
@{ Layout = "_LayoutForLessons.cshtml";}

@Styles.Render("~/Content/LessonsForStudents")
@using Bender.Extensions
@model RKE.UIModels.RozkladModelsForTeachers.RozkladModelForTeachersRozkladModel
        <div class="container">
    <div>@Model.FullName</div>
    <div class="col-md-12">
        @foreach (var weekItem in @Model.LessonForStudents)
        {
            <table class="table table-responsive table-striped table-bordered ">
                <tbody>
                    <tr>
                        <td></td>
                        <td>Понеділок</td>
                        <td>Вівторок</td>
                        <td>Середа</td>
                        <td>Четвер</td>
                        <td>П'ятниця</td>
                        <td>Субота</td>
                    </tr>
                    <tr>
                        @for (int i = 0; i < 6; i++)
                {
                        <tr>
                            <td></td>
                            @for (int j = 0; j < 6; j++)
                            {
                                <td class="col-md-2">
                                   
                                       
                                </td>

                            }
                        </tr>
                        }


                    </tbody>
                </table>
        }
    </div>
            
    @if (@Model.Session.IsNullOrEmpty())
    {
        <div class="col-md-4 table-responsive">
            <table class="table table-striped">
                <thead>
                <tr>
                    <th>Група</th>
                    <th>Дисципліна</th>
                    <th>Дата</th>
                    <th>Ауд.</th>
                </tr>
                </thead>
                <tbody>
                @foreach (var item in Model.Session)
                {
                    <tr>
                        <td></td>
                        <td>@item.NameOfLesson</td>
                        <td>@item.Date.ToShortDateString()</td>
                        <td>@item</td>
                        njxrb pjhe
                        точки зору

                    </tr>
                }
                </tbody>
            </table>
        </div>
    }
</div>
